# SCT_SD_2
Created a Number Guessing Game using Python. The program generates a random number, provides hints for each guess, and displays the total number of attempts required to find the correct number. Developed using Python concepts such as loops, conditions, user input, and random number generation.
